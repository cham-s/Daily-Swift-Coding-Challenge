func RomanNumeral(number: Int) -> String {
    let strNumber = String(number).reversed()
    let decomposed: [String] = []
    let power = 0
    
    for n in strNumber {
        switch power {
        case <#pattern#>:
            <#code#>
        default:
            <#code#>
        }
    }
}
